```json
{
    "main_function": {
        "name": "removeDuplicates",
        "purpose": "Removes duplicate elements from a non-strictly increasing array in-place, keeping the relative order of elements, and returns the new length of the array with unique elements.",
        "signature": {
            "parameters": [
                {
                    "name": "nums",
                    "type": "List[int]",
                    "description": "A non-strictly increasing array of integers with possible duplicates.",
                    "constraints": "Must be a non-strictly increasing array (each element is less than or equal to the next element)."
                }
            ],
            "return_type": "int",
            "return_description": "The number of unique elements in the modified array."
        },
        "exceptions": [
            {
                "type": "ValueError",
                "condition": "If the input array is not non-strictly increasing."
            }
        ],
        "complexity": "O(n) time complexity, O(1) space complexity."
    },
    "helper_functions": [],
    "design_notes": "The function modifies the input array in-place, and the elements beyond the returned length are not important. The relative order of the unique elements is preserved."
}
```